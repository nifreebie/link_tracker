app:
  telegram-token: ${TELEGRAM_TOKEN}

spring:
  application:
    name: Bot
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  config:
    import: optional:file:D:\java-nifreebie\bot\.env[.properties]

server:
  port: 8080

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui
